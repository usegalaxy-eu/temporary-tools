<tool id="mfa_find_oovs" name="MFA Find OOVs" version="@TOOL_VERSION@+@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>find words in a corpus that are missing from the dictionary</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="creators"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
        @PREPARE_ENV@
        @PREPARE_CORPUS@
        @PREPARE_DICTIONARY@

        ## 3. Run Find OOVs
        ## Syntax: mfa find_oovs [OPTIONS] CORPUS_DIRECTORY DICTIONARY_PATH OUTPUT_DIRECTORY
        mfa find_oovs
            corpus_dir
            $dict_arg
            output_dir
            --job_name 'mfa_find_oovs_job'
            #if str($speaker_characters):
                --speaker_characters '$speaker_characters'
            #end if
    ]]></command>
    <inputs>
        <expand macro="input_corpus"/>
        <expand macro="input_dictionary"/>
        <expand macro="common_inputs"/>
    </inputs>
    <outputs>
        <data name="oovs_found" format="txt" from_work_dir="output_dir/oovs_found.txt" label="${tool.name} on ${on_string}: OOV Words"/>
        <data name="oov_counts" format="tabular" from_work_dir="output_dir/oov_counts.txt" label="${tool.name} on ${on_string}: OOV Counts"/>
        <data name="utterance_oovs" format="txt" from_work_dir="output_dir/utterance_oovs.txt" label="${tool.name} on ${on_string}: Utterance OOVs"/>
    </outputs>
    <tests>
        <test expect_num_outputs="3">
            <conditional name="corpus_input">
                <param name="source" value="archive"/>
                <param name="corpus_archive" value="test_corpus.zip" ftype="mfa_corpus_model.zip"/>
            </conditional>
            <conditional name="dictionary_type">
                <param name="source" value="history"/>
                <param name="dictionary_file" value="test_dict.txt" ftype="mfa.dict"/>
            </conditional>
            <output name="oovs_found">
                <assert_contents>
                    <has_text text="hello"/>
                </assert_contents>
            </output>
            <output name="oov_counts">
                <assert_contents>
                    <has_text text="hello"/>
                </assert_contents>
            </output>
            <output name="utterance_oovs">
                <assert_contents>
                    <has_text text="speaker"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
**Montreal Forced Aligner: Find OOVs**

This tool scans your corpus and compares the words in your transcripts against the provided pronunciation dictionary.

**What it does**

It identifies **Out-Of-Vocabulary (OOV)** items—words that exist in your text but missing from the dictionary. If a word is OOV, MFA cannot align it because it doesn't know its phonemes.

**Outputs**

* **OOV Words:** A list of missing words (`oovs_found.txt`). Feed this directly into the **MFA G2P** tool to generate pronunciations.
* **OOV Counts:** How many times each missing word appears.
* **Utterance OOVs:** Which specific recording files contain the missing words.

    ]]></help>
    <expand macro="citations"/>
</tool>
