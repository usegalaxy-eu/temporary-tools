<tool id="mfa_train_dictionary" name="MFA Train Dictionary" version="@TOOL_VERSION@+@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>calculate pronunciation probabilities for a dictionary</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="creators"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
        @PREPARE_ENV@
        @PREPARE_CORPUS@
        @PREPARE_DICTIONARY@
        @PREPARE_ACOUSTIC@

        ## 4. Run Train Dictionary
        ## Syntax: mfa train_dictionary [OPTIONS] CORPUS_DIRECTORY DICTIONARY_PATH ACOUSTIC_MODEL_PATH OUTPUT_DIRECTORY
        mfa train_dictionary
            corpus_dir
            $dict_arg
            $ac_arg
            output_dir
            --job_name 'mfa_train_dict_job'
            #if str($silence_probabilities) == "true":
                --silence_probabilities
            #end if

        ## 5. Move Result
        ## The new dictionary (with probabilities) is saved in the output_dir.
        ## We pick the first .dict or .txt file MFA produced.
        &&
        mv "$(find output_dir -maxdepth 1 -type f \( -name '*.dict' -o -name '*.txt' \) | head -1)" '$output_dictionary'
    ]]></command>
    <inputs>
        <expand macro="input_corpus"/>
        <expand macro="input_dictionary"/>
        <expand macro="input_acoustic"/>
        <param argument="--silence_probabilities" type="boolean" truevalue="--silence_probabilities" falsevalue="" checked="true" label="Calculate Silence Probabilities" help="If checked, MFA will also calculate how likely silence is to occur around specific words."/>
    </inputs>
    <outputs>
        <data name="output_dictionary" format="mfa.dict" label="${tool.name} on ${on_string}: Probabilistic Dictionary"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <conditional name="corpus_input">
                <param name="source" value="archive"/>
                <param name="corpus_archive" value="test_corpus.zip" ftype="mfa_corpus_model.zip"/>
            </conditional>
            <conditional name="dictionary_type">
                <param name="source" value="history"/>
                <param name="dictionary_file" location="https://github.com/MontrealCorpusTools/mfa-models/releases/download/dictionary-english_us_arpa-v3.0.0/english_us_arpa.dict" ftype="mfa.dict"/>
            </conditional>
            <conditional name="acoustic_type">
                <param name="source" value="history"/>
                <param name="acoustic_file" location="https://github.com/MontrealCorpusTools/mfa-models/releases/download/acoustic-english_us_arpa-v3.0.0/english_us_arpa.zip" ftype="mfa_acoustic_model.zip"/>
            </conditional>
            <assert_stdout>
                <has_text text="Found 5 speakers"/>
                <has_text text="Done!"/>
            </assert_stdout>
            <output name="output_dictionary">
                <assert_contents>
                    <has_text text="hello"/>
                    <has_text_matching expression="\s[01]\.\d+"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
**Montreal Forced Aligner: Train Dictionary**

This tool upgrades a standard pronunciation dictionary into a **Probabilistic Dictionary**.

**How it works**

It runs an alignment on your corpus using the provided acoustic model. It then counts how often each pronunciation variant is used. 
For example, if the word "data" appears 100 times:

* 80 times it was pronounced /d ey t ax/
* 20 times it was pronounced /d ae t ax/

The output dictionary will record these probabilities (0.8 and 0.2).

**Benefit**

Using a probabilistic dictionary in **MFA Align** usually results in higher accuracy because the aligner knows which pronunciations are more likely.

**Inputs**

1. **Corpus:** Audio and Transcripts.
2. **Dictionary:** The starting dictionary.
3. **Acoustic Model:** Needed to perform the alignment check.

**Output**

* A new `.dict` or `.txt` file containing the dictionary with probability columns added.

    ]]></help>
    <expand macro="citations"/>
</tool>
